# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  ## using vagrant cloud
  config.vm.box = "hfm4/sl6"

  ## using vagrantbox built by oneself
  ## `packer build packer.json` before vagrant up
  # config.vm.box = "sl6"
  # config.vm.box_url = "./sl-6.box"
end
